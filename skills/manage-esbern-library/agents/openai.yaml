interface:
  display_name: "Manage Esbern Library"
  short_description: "Search, add, push, and pull Esbern books"
  default_prompt: "Use $manage-esbern-library to search my library, add a missing book, or explicitly push, pull, or synchronize it with reMarkable."
